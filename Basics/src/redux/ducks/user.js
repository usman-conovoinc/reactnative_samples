// @ts-nocheck
/*
export const GET_USERS = "GET_USERS"
const SET_USERS = "SET_USERS"

export const setUsers = (users, loading) => ({
    type: SET_USERS,
    users: users,
    loading: loading
});

export const getUsers = () => ({
    type: GET_USERS,
    loading: true
});

const initialState = {
    users: [],
    loading: false
};

export default (state = initialState, action) => {
    switch (action.type) {
        case SET_USERS:
            const { users, loading } = action;
            return { ...state, users, loading };
        default:
            return state;
    }
}
*/